<%= form_for([@issue, @issue.options.build]) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :option, placeholder: "New Option...", :autocomplete => :off %>
  </div>
  <%= f.submit "Create", class: "btn btn-primary" %>
<% end %>